id: mqtt_client
broker: $MQTT_BROKER
username: $MQTT_USERNAME
password: $MQTT_PASSWORD
discovery_prefix: ${MQTT_DISCOVERY_PREFIX}homeassistant
topic_prefix: device/$NAME
reboot_timeout: $REBOOT_TIMEOUT
